streamlit>=1.53.0,<2
pandas>=2.0
numpy>=1.24
plotly>=5.18
Pillow>=10.0
python-pptx>=0.6.23
reportlab>=4.0
openpyxl>=3.1
xlsxwriter>=3.1
streamlit-authenticator==0.3.2
PyYAML==6.0.2
bcrypt>=4.1.0
requests
google-auth==2.40.3
google-auth-oauthlib==1.2.2
google-api-python-client
kaleido==0.2.1
requests-oauthlib==2.0.0

# ddr-rogii OCR SIN packages.txt. La imagen de Streamlit Cloud (Trixie) arrastra un
# source de bullseye-security y su apt-get update sale con codigo distinto de cero:
# en cuanto este repo tiene packages.txt, Cloud aborta la preparacion del entorno con
# "Error installing requirements" y la app entera queda caida. No se arregla desde el
# repo (las fuentes apt son de la imagen), asi que no hay packages.txt y OpenCV va en
# rueda headless, que no enlaza libGL ni libgthread y no necesita apt.
# El despliegue de Dokploy (ddr-reportapp.i.solo.cloud) instala libgl1 y libglib2.0-0
# en su propio Dockerfile y no depende de nada de esto.
# Nota: rapidocr declara opencv-python como dependencia, asi que pip bajara tambien la
# rueda completa y las dos escriben el mismo cv2/. Si el OCR de PDFs escaneados vuelve
# a fallar por libGL aqui, es por eso; el resto de la app no se ve afectada.
pdfplumber>=0.10
pymupdf>=1.23
onnxruntime>=1.17
opencv-python-headless>=4.8,<6

# rapidocr-onnxruntime esta capado a Python <3.13 en todas sus releases. En 3.13 pip
# no encuentra ninguna version y aborta la instalacion entera del entorno
# ("Error installing requirements"). rapidocr es el sucesor y si soporta 3.13.
rapidocr-onnxruntime>=1.4.2,<3; python_version < "3.13"
rapidocr>=2.0,<4; python_version >= "3.13"

